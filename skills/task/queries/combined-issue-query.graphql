{ repository(owner: "<owner>", name: "<repo>") {
    issue(number: <issueNumber>) {
      id
      blockedBy(first: 100) {
        nodes { number state stateReason }
        pageInfo { hasNextPage endCursor }
      }
      parent {
        number title
        subIssues(first: 100) {
          nodes { number title state }
          pageInfo { hasNextPage endCursor }
        }
      }
      linkedBranches(first: 5) { nodes { id ref { name } } }
    }
  }
}
