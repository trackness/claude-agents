<!--
READY INVARIANT — the single definition of "Ready", cited by /audit and /promote.
An issue is Ready only when ALL of these hold:
  1. Every section below is filled in per this template — no placeholders, no "TBD", no unfilled slots.
  2. Effort is set.
  3. Priority is set.
  4. Type is set.
  5. It is not blocked by any open issue.
/audit's fast-lane Ready drafts and /promote both enforce this before setting Status = Ready.
This comment is documentation for the template; it is not part of an issue body.
-->

## Why
Concrete explanation of the problem and user impact.

## Implementation
Specific file locations, code to add, commands to run.
*Treat as guidance — verify against current code state.*

## Files
`path/to/file` (modify), `path/to/new` (new)

## Testing
Commands or steps to verify.

## Acceptance Criteria
- [ ] Criterion one
- [ ] Criterion two
